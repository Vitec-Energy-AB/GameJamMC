# This directory holds the runtime leaderboard JSON file (data/leaderboard.json).
# The JSON file itself is excluded by .gitignore and will be created automatically
# when the server first receives a score submission.
