// Shared player physics constants used across server modules
export const PLAYER_WIDTH = 80;
export const PLAYER_HEIGHT = 120;

/** Semantic version of the game – included in leaderboard entries */
export const GAME_VERSION = '1.0.0';

/** Height of the active game window above the lava surface (in pixels) */
export const WINDOW_HEIGHT = 1400;
